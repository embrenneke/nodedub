CREATE TABLE registrations (id SERIAL PRIMARY KEY NOT NULL, time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, confirmed BOOLEAN NOT NULL DEFAULT false, emailed BOOLEAN NOT NULL DEFAULT false, nonce VARCHAR(8) NOT NULL, email VARCHAR(320) NOT NULL UNIQUE);
